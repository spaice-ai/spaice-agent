#!/bin/bash
# Instinct Learning CLI Wrapper

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
INSTINCT_MANAGER="$SCRIPT_DIR/instinct-manager.py"

# Make the Python script executable
chmod +x "$INSTINCT_MANAGER"

# Pass all arguments to the Python script
python3 "$INSTINCT_MANAGER" "$@"